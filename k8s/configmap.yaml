apiVersion: v1
kind: ConfigMap
metadata:
  name: planka-config
  namespace: planka-system
data:
  BASE_URL: "http://planka.local"
  DATABASE_URL: "postgresql://postgres:password123@postgres-statefulset-0.postgres-service.planka-system.svc.cluster.local:5432/planka"  # Redis is optional but included
  REDIS_URL: "redis://redis-service:6379"